[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kemetic-os"
version = "0.1.0"
description = "Kemetic OS — modular reference architecture for agent state loops"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name="Nile Green"}]
dependencies = []

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
include = ["kemetic_os*"]
